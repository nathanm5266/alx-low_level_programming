#include "holberton.h"

/** 
 * swap_int - a function that swaps values
 * @a: a variable for accepting an intiger
 * @b: a variable for accepting an intiger
 */

void swap_int(int *a, int *b);
{
int n;
n = *a;
*a = *b;
*b = n;
}
